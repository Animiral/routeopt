/// Contains additional functionalities which are shared across tests.
#pragma once

#include "Graph.h"

/**
 * Return true if the paths contain the same nodes.
 */
bool IsPathEqual(const Path& expected, const Path& actual);
